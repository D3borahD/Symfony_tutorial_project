<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Mon titre</title>
    </head>
    <body>
    <p>test</p>
   {#     <h1>Demande de {{ contact.fullName }}</h1>
        <p>Email : {{ contact.email }}</p>
        <p>Sujet :
            {{ contact.subject }}</p>
        <p>Message :</p>
        <div>
            {{ contact.message|raw }}
        </div>#}
    </body>
</html>
